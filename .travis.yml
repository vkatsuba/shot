language: erlang
dist: xenial
sudo: required
addons:
    apt:
        packages:
        #- build-essential
        - sqlite3
    hosts:
        - localhost
before_install:
        - git config --global url."git://github".insteadOf https://github
#install:
#        - xxx
otp_release:
        - 22.0
env:
  global:
    - DEBUG=on

# blocklist
branches:
  except:
  - master
